apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-executor.fullname" . }}-api
  labels:
    {{- include "code-executor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "code-executor.selectorLabels" . | nindent 4 }}
